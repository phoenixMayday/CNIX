char* arr = alloc(10);

arr[0] = 65;  // 'A'
arr[1] = 66;  // 'B'
arr[2] = 67;  // 'C'

qword a = arr[0];
qword b = arr[1];
qword c = arr[2];

_ = free(arr, 10);

exit(a + b + c);  // 65 + 66 + 67 = 198
